"use client";

import FormWrapper from "@/app/blog/_components/FormWrapper";

const NoticeCreate = () => {
  return <FormWrapper title="공지사항 등록" name="notice" editMode={false} />;
};

export default NoticeCreate;
